import MessageType from "../../model/MessageType"

type Props = {
    message: MessageType
}

const MessageSimple: React.FC<Props> = ({ message }) => {

    return <div>m s</div>
}
export default MessageSimple